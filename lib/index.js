// Generated by CoffeeScript 1.6.3
var Model, racer, toast;

racer = require('racer');

Model = racer.Model || racer["protected"].Model;

toast = module.exports;

toast.init = function(app) {
  Model.prototype.toast = function(type, msg) {
    return this.push("_page.toast." + type, msg);
  };
  app.fn('toast.remove', function(e, el) {
    console.log(app.model.at(el).path().substring(0, 12));
    if (app.model.at(el).path().substring(0, 12) === '_page.toast.') {
      return app.model.at(el).remove();
    }
  });
  return app;
};
